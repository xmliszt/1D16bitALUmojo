module decimalToBinary (
    input value,
    output out[7]
  ) {
  
  sig temVal;
  var i;
  var remainder;

  always {
    temVal = value;
    for (i = 0; i < 7; i ++){
      remainder = temVal - (temVal/2)*2;
      out[i] = remainder;
      temVal = temVal/2;  
      }
  }
}
